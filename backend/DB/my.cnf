[mysqld]
# CRITICAL: Bind to all network interfaces for Docker
bind-address = 0.0.0.0
port = 3306

# Performance
skip-name-resolve
max_connections = 1000

# InnoDB
default-storage-engine = InnoDB

# Charset
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[client]
port = 3306
